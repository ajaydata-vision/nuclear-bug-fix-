# Verification

## Before Fix

useQuery → undefined, no fetch

## After Fix

After TanStack Query v5 upgrade → hooks work correctly

## Regression Checks

Update all react-query API usage for v5 breaking changes
